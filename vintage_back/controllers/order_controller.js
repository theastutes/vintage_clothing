
import mongoose from "mongoose";
import { Order } from "../models/User";
export const createOrder = async (req,res) =>{
    try {
        // const {productId,name,email,}
        
    } catch (error) {
        
    }
}